from fastapi import FastAPI
import os

app = FastAPI(title="Customer Feedback API")

@app.get("/")
def home():
    return {"status": "Active", "mode": "Initial Base Code"}

@app.post("/feedback")
def receive_feedback(customer_id: str, text: str):
    # Base code only logs the text. AI analysis is missing!
    return {"message": "Feedback received", "customer": customer_id}
